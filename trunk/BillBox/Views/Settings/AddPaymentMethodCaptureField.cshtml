@model BillBox.Models.PaymentMethodCaptureField

@{
    ViewBag.Title = "Add Payment Method Capture Field";
    Layout = "~/Views/Shared/_Default.cshtml";
}

<div class="action">
    <div class="section">
        <div class="section-header">
        </div>
        <div class="section-content">
            <div class="form-container">
                @using (Html.BeginForm("AddPaymentMethodCaptureField","Settings", FormMethod.Post, new { @class = "wf" })) {
                    @Html.AntiForgeryToken()
                    @Html.ValidationSummary(true)

                    @Html.HiddenFor(m => m.PaymentMethodId)

                    <fieldset class="top">
                        <legend>Payment Method Capture Field</legend>
                        <div class="row top clearfix">
                                @Html.LabelFor(m => m.Name)
                                @Html.TextBoxFor(m => m.Name)
                                @Html.ValidationMessageFor(m => m.Name)
                        </div>
                        <div class="row clearfix">
                                @Html.LabelFor(m => m.DisplayName)
                                @Html.TextBoxFor(m => m.DisplayName)
                                @Html.ValidationMessageFor(m => m.DisplayName)
                        </div>
                        <div class="row clearfix">
                                @Html.LabelFor(m => m.Type)
                                @Html.TextBoxFor(m => m.Type)
                                @Html.ValidationMessageFor(m => m.Type)
                        </div>
                        <div class="row bottom clearfix">
                                @Html.LabelFor(m => m.OrderNum)
                                @Html.TextBoxFor(m => m.OrderNum)
                                @Html.ValidationMessageFor(m => m.OrderNum)
                        </div>
        
                    </fieldset>
                    <div class="buttons bottom">
                            <input type="submit" value="Create" />
                    </div>
                }
            </div>
        </div>
        <div class="section-footer">
        </div>
    </div>
</div>

@section SidebarLeft {
    @Html.Partial("_SidebarMenu")
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}

